import { RouteConfig } from './types';
import homeRoute from '../pages/home/route.config';

// Import all route configs here
export const routes: RouteConfig[] = [
  homeRoute,
  // Add more routes here as needed
]; 